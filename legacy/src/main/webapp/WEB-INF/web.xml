<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>Legacy Application with Azure AD OAuth2</display-name>
    
    <description>
        Legacy Java web application with direct Azure AD OAuth2 integration.
    </description>

    <!-- ========================================== -->
    <!-- AZURE AD OAUTH2 CALLBACK FILTER           -->
    <!-- Handles token exchange from Azure AD      -->
    <!-- MUST BE FIRST - order matters!             -->
    <!-- ========================================== -->
    <filter>
        <filter-name>AzureADCallbackFilter</filter-name>
        <filter-class>com.example.legacyapp.filter.AzureADCallbackFilter</filter-class>
        
        <init-param>
            <param-name>azureAd.clientId</param-name>
            <param-value>${AZURE_CLIENT_ID}</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.clientSecret</param-name>
            <param-value>${AZURE_CLIENT_SECRET}</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.tenantId</param-name>
            <param-value>${AZURE_TENANT_ID}</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.redirectUri</param-name>
            <param-value>https://domain/my-app/login/oauth2/code/azure</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.scope</param-name>
            <param-value>openid profile email</param-value>
        </init-param>
    </filter>

    <!-- Map callback filter to exact callback path -->
    <filter-mapping>
        <filter-name>AzureADCallbackFilter</filter-name>
        <url-pattern>/login/oauth2/code/azure</url-pattern>
        <dispatcher>REQUEST</dispatcher>
    </filter-mapping>

    <!-- ========================================== -->
    <!-- AZURE AD OAUTH2 AUTHENTICATION FILTER     -->
    <!-- Checks authentication and initiates flow  -->
    <!-- ========================================== -->
    <filter>
        <filter-name>AzureADOAuth2Filter</filter-name>
        <filter-class>com.example.legacyapp.filter.AzureADOAuth2Filter</filter-class>
        
        <init-param>
            <param-name>azureAd.clientId</param-name>
            <param-value>${AZURE_CLIENT_ID}</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.tenantId</param-name>
            <param-value>${AZURE_TENANT_ID}</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.redirectUri</param-name>
            <param-value>https://domain/my-app/login/oauth2/code/azure</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.scope</param-name>
            <param-value>openid profile email</param-value>
        </init-param>
        
        <init-param>
            <param-name>azureAd.callbackPath</param-name>
            <param-value>/login/oauth2/code/azure</param-value>
        </init-param>
    </filter>

    <!-- Apply authentication filter to all requests except callback -->
    <filter-mapping>
        <filter-name>AzureADOAuth2Filter</filter-name>
        <url-pattern>/*</url-pattern>
        <dispatcher>REQUEST</dispatcher>
        <dispatcher>FORWARD</dispatcher>
    </filter-mapping>

    <!-- Welcome Files -->
    <welcome-file-list>
        <welcome-file>home.jsp</welcome-file>
        <welcome-file>index.jsp</welcome-file>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <!-- Session Configuration -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <!-- <secure>true</secure> --> <!-- Enable in production -->
        </cookie-config>
    </session-config>

    <!-- Error Pages -->
    <error-page>
        <error-code>404</error-code>
        <location>/error.html</location>
    </error-page>
    
    <error-page>
        <error-code>500</error-code>
        <location>/error.html</location>
    </error-page>
    
    <error-page>
        <exception-type>java.lang.Exception</exception-type>
        <location>/error.html</location>
    </error-page>

    <!-- MIME Type Mappings -->
    <mime-mapping>
        <extension>css</extension>
        <mime-type>text/css</mime-type>
    </mime-mapping>
    
    <mime-mapping>
        <extension>js</extension>
        <mime-type>application/javascript</mime-type>
    </mime-mapping>
    
    <mime-mapping>
        <extension>json</extension>
        <mime-type>application/json</mime-type>
    </mime-mapping>

</web-app>